# Lakehouse KG Starter — base (CPU) dependencies.
# The agentic pipeline is the standalone canonical package; this kit consumes it.
agentic-triplets[spark,llm]
# CPU graph algorithms:
#   notebook 06 — networkx (driver, serverless-safe)
#   notebook 08 — graphframes-serverless (distributed, runs on serverless too)
networkx>=3.0
graphframes-serverless>=0.3.0
